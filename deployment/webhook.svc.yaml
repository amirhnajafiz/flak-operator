apiVersion: v1
kind: Service
metadata:
  labels:
    app: flak
  name: flak-operator-service
  namespace: flak-operator
spec:
  type: ClusterIP
  ports:
    - port: 443
      protocol: TCP
      targetPort: 443
  selector:
    app: flak-operator
